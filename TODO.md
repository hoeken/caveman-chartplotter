# Initial Release - 1.0

- add read-only routes from SignalK routes provider to the chart.
  - simple lines with a dot at each waypoint.

- new appstore screenshots (`signalk.screenshots` in package.json is currently empty)