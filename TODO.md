# Initial Release - 1.0

- increase the size of the UI icons (config, layers, theme, home, zoom) to be 2x the current width and height.

- new appstore screenshots (`signalk.screenshots` in package.json is currently empty)